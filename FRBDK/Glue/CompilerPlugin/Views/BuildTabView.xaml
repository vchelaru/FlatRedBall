<UserControl x:Class="CompilerPlugin.Views.BuildTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:CompilerPlugin.Views"
             xmlns:localization="clr-namespace:Localization;assembly=Localization"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <WrapPanel Orientation="Horizontal" Visibility="{Binding EntireUiVisibility}">
            <Button Click="Button_Click" Content="{materialDesign:PackIcon TextBoxRemove}" Style="{DynamicResource ToolIcon}">
                <Button.ToolTip>
                    <TextBlock>Clear Output</TextBlock>
                </Button.ToolTip>
            </Button>
            <views:WhileStoppedView
                Visibility="{Binding WhileStoppedViewVisibility}"
                BuildClicked="HandleCompileClick"
                PackageClicked="WhileStoppedView_PackageClicked"
                >
            </views:WhileStoppedView>

            <StackPanel x:Name="WhileBuildingView" Visibility="{Binding WhileBuildingVisibility}" Orientation="Horizontal">
                <Label Content="Building..."></Label>
                <Button Click="HandleCancelBuildButtonClick" Content="Cancel Build"></Button>
            </StackPanel>

            <CheckBox Content="Embedded diagnostics" VerticalAlignment="Center" Margin="6,0,0,0"
                      IsChecked="{Binding IsEmbeddedDiagnosticsChecked}">
                <CheckBox.ToolTip>
                    <TextBlock>
                        Logs every DTO the game receives and its response, click attempts (with the<LineBreak/>
                        focus-gate values that decided whether they were processed), and selection changes<LineBreak/>
                        from the embedded game window to a file. Use this when a live-edit action silently<LineBreak/>
                        fails or does nothing, on a specific machine or otherwise.
                    </TextBlock>
                </CheckBox.ToolTip>
            </CheckBox>

            <Button Content="Open Diagnostics Log" VerticalAlignment="Center" Margin="6,0,0,0"
                    Click="HandleOpenEmbeddedDiagnosticsLogButtonClick">
                <Button.ToolTip>
                    <TextBlock>Opens the current game session's embedded diagnostics log file (see the checkbox above).</TextBlock>
                </Button.ToolTip>
            </Button>

            <Button VerticalAlignment="Center" Margin="12,0,0,0" Click="HandleMSBuildSettingsButtonClick"
                    Content="{materialDesign:PackIcon Cog}" Style="{DynamicResource ToolIcon}"
                    Visibility="{Binding WhileStoppedViewVisibility}">
                <Button.ToolTip>
                    <TextBlock>Build Settings</TextBlock>
                </Button.ToolTip>
            </Button>

        </WrapPanel>

        <RichTextBox Grid.Row="2" ScrollViewer.VerticalScrollBarVisibility="Auto" 
                 ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                 x:Name="TextBox"> </RichTextBox>
    </Grid>
</UserControl>
